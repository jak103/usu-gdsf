import { mount } from '@vue/test-utils'
import menu from './NavMenu.vue'

test('renders Nav Menu', () => {
//   const wrapper = mount(menu)

//   const todo = wrapper.get('[data-test="defaultGame"]')

//   expect(todo).toBe('Learn Vue.js 3')

expect(1).toEqual(1);
})